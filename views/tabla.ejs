<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6"
      crossorigin="anonymous"
    />
    <title>Todos los productos</title>
  </head>
  <body>
    <div class="container mt-3">
      <% if(resultados.length >= 1) { %>
      <table class="table table-bordered table-sm">
        <thead>
          <tr>
            <th class="align-middle" scope="col">Nombre</th>
            <th class="align-middle" scope="col">Precio</th>
            <th class="align-middle" scope="col">Descripción</th>
            <th class="align-middle" scope="col">Imágen</th>
          </tr>
        </thead>
        <tbody>
          <% resultados.forEach((producto)=>{ %>
          <tr>
            <td class="align-middle"><%=producto.nombre%></td>
            <td class="align-middle">$<%=producto.precio%></td>
            <td class="align-middle"><%=producto.descripcion%></td>
            <td class="align-middle">
              <img
                src="<%= producto.urlFoto %>"
                id="imagenProducto"
                alt="imágen del producto"
                class="img-fluid"
              />
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } else { %>
      <div class="alert alert-dark" role="alert">
        No hay productos para listar!
        <a href="/" class="alert-link">Haz clic aquí para agregar un producto</a
        >.
      </div>
      <% } %>
    </div>
  </body>
</html>
